#include "lib_irc.hpp"

void cmdPrivmsg(Server &server ,RawTextLine &line, Client &client)
{
    // Check if the destination is User, Channel

    // search_user() Look up the user in the server's client list;
        // send message to client

        // else error
    
    // search_channel() Look up the user in the server's channel list;
        // send message to channel

        // else error
}