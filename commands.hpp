#ifndef COMMANDS_HPP
#define COMMANDS_HPP

/* CHANNEL*****************************************************************/
/*
/ * INVITE   - Invite a client to an invite-only channel (mode +i)
/ * MODE     - Change the channel's mode, as well as members' privileges
/ / PRIVMASG - Sending messages to the channel (mode +n, +m, +v)
/ / TOPIC    - Change the channel topic in a mode +t channel
/ */

#define INVITE
#define KICK
#define MODE
#define PRIVMSG
#define TPOIC
/**************************************************************************/

/* USER********************************************************************/
/*
/ * INVITE   - Invite a client to an invite-only channel (mode +i)
/ * MODE     - Change the channel's mode, as well as members' privileges
/ / PRIVMASG - Sending messages to the channel (mode +n, +m, +v)
/ / TOPIC    - Change the channel topic in a mode +t channel
/ */

#define JOIN
#define OPER
#define QUIT
#define PRIVMSG
#define TOPIC
/**************************************************************************/

#endif